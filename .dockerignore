# 빌드 산출물 / 캐시 (이미지 안에서 새로 빌드하므로 제외)
build/
.gradle/
bin/
out/

# 로컬 시크릿 (컨테이너는 환경변수로 주입받음 → 이미지에 포함 금지)
src/main/resources/application-secret.yml
.env
.env.*
!.env.example

# 업로드 데이터
uploads/

# git / IDE / OS
.git/
.gitignore
.github/
.idea/
*.iml
.vscode/
.DS_Store

# 문서
*.md
