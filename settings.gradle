rootProject.name = 'duo-backend'

// 서비스 경계 = 배포 단위. common 은 라이브러리이고 나머지는 각각 독립 부트 앱이다.
// match / crew 는 기능 개발이 시작되면 여기에 추가한다.
include 'common'
include 'user'
include 'post'
include 'chat'
include 'riot'
include 'match'